var str = "472";

document.write(
    "<h1>"+
    "Value: "+ `${str}`+
    "</br>Type: "+`${typeof(str)}`+
    "</h1>"
);
var number = parseInt(str);

document.write(
    "<h1>"+
    "Value: "+ `${number}`+
    "</br>Type: "+`${typeof(number)}`+
    "</h1>"
);